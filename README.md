# BeautifulSoup4 webscraper built to analyze and display user's top stats on Film site Letterboxd
Uses Flask to link Python backend script with HTML frontend built with TailwindCSS and a virtual environment

